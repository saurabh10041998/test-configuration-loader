[http]
host = "0.0.0.0"
port = 8080

[auth]
jwt_secret = "change-me-in-production-256-bit-secret"

[mail]
smtp_host = "smtp.example.com"
smtp_port = 587
smtp_username = "noreply@example.com"
smtp_password = "smtp-password"
from_address = "noreply@example.com"
use_tls = true
